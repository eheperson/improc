
from gui import Window



w = Window()
w.display()

# p.resize(scale=0.5)
# p.display()

# p.quantizer()
# p.quantizer(quant=8)
# p.quantizer(quant=64)
# p.quantizer(quant=128)

# p.sampler()
# p.sampler(ratio=2)
# p.sampler(ratio=4)
# p.sampler(ratio=8)
# p.sampler(ratio=16)
# p.sampler(ratio=32)
